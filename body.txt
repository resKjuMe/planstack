{"data":{"id":575,"name":"F1","summary":"Neuer zentraler Dienst schreibt Benutzer- und Request-Informationen in den SQL-Sitzungskontext","acceptance_criteria":"Scope:\n- Nur der neue Dienst + Registrierung; noch keine Aufrufer (Middleware\/Collector folgen in F2-F4)\nDone when:\n- AuditContextService setzt users_id, support_users_id, request_id und group_id per sp_set_session_context auf der geteilten Verbindung\n- clear() setzt alle vier Schl\u00fcssel auf NULL\n- Integrationstest liest die gesetzten Werte via SELECT SESSION_CONTEXT(N'...') zur\u00fcck\n- Vollst\u00e4ndige Testabdeckung f\u00fcr den neuen Code\nContract:\n- Schl\u00fcsselnamen exakt: users_id, support_users_id, request_id, group_id (die Trigger aus Phase 2 lesen genau diese)","target_actual":"IST: Im SQL-Sitzungskontext steht nur die Firmen-Id (companies_id); Benutzer, Support-Benutzer und Request lassen sich auf Datenbank-Ebene nicht zuordnen.\nSOLL: Ein zentraler Dienst kann zus\u00e4tzlich Benutzer-Id, Support-Benutzer-Id, Request-Id und Gruppen-Id in den SQL-Sitzungskontext schreiben und wieder l\u00f6schen.","test_cases":"1. Integrationstest (Dev-DB): AuditContextService::setRequestContext(42, 7, '<uuid>') aufrufen\n2. Per SQL SELECT SESSION_CONTEXT(N'users_id') usw. auslesen\nErwartung: Die gesetzten Werte kommen exakt zur\u00fcck; nach clear() liefern alle Schl\u00fcssel NULL\nHinweis: Nur per Test pr\u00fcfbar, keine sichtbare Oberfl\u00e4chen-\u00c4nderung","criticality":"low","criticality_label":"low","status":"IN_PROGRESS","display_status":"IN_PROGRESS","phase_id":53,"effort":{"man_days":null,"story_points":2,"tokens":60000},"custom_fields":null,"pr_number":"8531","pr_url":"https:\/\/github.com\/clockodo-intern\/main\/pull\/8531","claimed_by_id":7,"last_reviewed_at":"2026-07-23T10:49:49.000000Z","last_review_recommendation":"REQUEST_CHANGES","last_review_summary":"Review-Konfiguration: Strenge=strict, Gr\u00fcndlichkeit=meticulous, Modell=Opus 4.8 (1M-Kontext), Effort=high\n\nTLDR: Der Dienst ist fachlich korrekt und sicher und folgt sauber dem etablierten `DbSecurityService`-Muster; wegen dreier Testm\u00e4ngel (falsche Test-Suite\/Basisklasse, Namenskonvention verletzt, fehlender Escaping-Edge-Case) bei strenger Bewertung jedoch `REQUEST_CHANGES`.\n\n## Ausf\u00fchrliche Analyse\n\n### `AuditContextService.php` \u2014 Produktivcode: solide\n- **SQL-Sicherheit korrekt.** Die Anweisung wird bewusst unprepared ausgef\u00fchrt (wie im `DbSecurityService`, da `sp_set_session_context` `@value` nicht als gebundenen Parameter akzeptiert). Die einzige Ausbruchsstelle aus dem T-SQL-String-Literal \u2014 das einfache Anf\u00fchrungszeichen \u2014 wird in `toSqlLiteral()` per `str_replace(\"'\", \"''\")` verdoppelt; das ist das korrekte und vollst\u00e4ndige T-SQL-Escaping (kein Backslash-Escaping in T-SQL). `$key` stammt ausschlie\u00dflich aus Klassenkonstanten, Ints werden typgepr\u00fcft als Literal ausgegeben. Kein Injection-Vektor erkennbar.\n- **Muster-Treue.** `getPdo()` bezieht die PDO \u00fcber `SharedEngineConnection::forAdapter($this->dbAdapter)->getPdo()` \u2014 identisch zum `DbSecurityService`, damit der Kontext auf derselben physischen Verbindung wie die Schreiboperationen gilt. Die Aufteilung in `setRequestContext()` \/ `setGroupId()` \/ `clear()` ist nachvollziehbar dokumentiert.\n- **Vertrag erf\u00fcllt.** Alle vier Schl\u00fcsselnamen (`users_id`, `support_users_id`, `request_id`, `group_id`) exakt wie gefordert; `clear()` setzt alle vier auf NULL (Connection-Pooling). NULL-Semantik (`@value = null` entfernt den Schl\u00fcssel) wird durch die Tests best\u00e4tigt.\n- **\u201eRegistrierung\" (Scope-Item).** Kein expliziter Factory-Eintrag n\u00f6tig: Der Dienst wird \u2014 wie `DbSecurityService` selbst \u2014 \u00fcber die `ReflectionBasedFactory` autowired (Konstruktor nur mit `Adapter`). Der gr\u00fcne Integrationstest, der den Dienst via `getService()` aufl\u00f6st, belegt das. Scope-Item damit erf\u00fcllt.\n- Kleiner, nicht blockierender Hinweis: `setRequestContext()`\/`clear()` setzen die Schl\u00fcssel in 3 bzw. 4 einzelnen `exec()`-Roundtrips; b\u00fcndelbar in ein Statement. Da nur einmal je Request, vernachl\u00e4ssigbar \u2014 keine \u00c4nderung gefordert.\n\n### `AuditContextServiceTest.php` \u2014 drei M\u00e4ngel (Grund f\u00fcr REQUEST_CHANGES)\n\n1. **Falsche Test-Suite und veraltete Basisklasse.** Der Test liegt unter `tests\/Integration\/` und erweitert `LaminasDbTestCase`. Laut `backend\/AGENTS.md:207,211` geh\u00f6ren neue Service+DB-Tests nach `IntegrationParallel\/` und `Integration\/` \u201eshould no longer be used\". Bitte Datei nach `IntegrationParallel\/Service\/` verschieben, Namespace anpassen und `Tests\\Support\\TestCases\\TestCase` erweitern. Dasselbe Shared-Connection-Muster zeigt `backend\/module\/App\/tests\/IntegrationParallel\/Utility\/Db\/Illuminate\/SharedEngineConnectionTest.php`.\n\n2. **Namenskonvention verletzt (alle 5 Methoden).** `backend\/AGENTS.md:220-225` verlangt `test_[For<Prefix>_]If<Szenario>_Then<Ergebnis>`. Allen f\u00fcnf Methoden fehlt das obligatorische `If<Szenario>`-Segment (z. B. `test_ForSetRequestContext_Then\u2026`, `test_ForClear_ThenAllFourKeysAreNull`). Anfangszustand explizit als `If\u2026` erg\u00e4nzen.\n\n3. **Escaping-Zweig ungetestet (sicherheitsrelevant).** Der die unprepared SQL absichernde `str_replace`-Zweig wird von keinem Test mit einem tats\u00e4chlichen Anf\u00fchrungszeichen durchlaufen \u2014 eine Regression im Escaping bliebe unbemerkt. Die Acceptance-Kriterien fordern explizit \u201eWerte werden sicher als Literal escaped\" und \u201eVollst\u00e4ndige Testabdeckung f\u00fcr den neuen Code\". Bitte einen Integrationsfall erg\u00e4nzen, der z. B. `request'id` setzt und pr\u00fcft, dass `SESSION_CONTEXT` den Originalstring exakt zur\u00fcckliefert.\n\n### Sonstiges\n- CI ist gr\u00fcn (BE Unit\/Integration\/PHPStan u. a.); die Konventions-Guard-Tests (`CodeInspection\/TestStructureTest`) laufen in einer separaten, hier offenbar nicht getriggerten Suite \u2014 die Verst\u00f6\u00dfe 1+2 fallen daher erst im Review auf.\n- Die drei Punkte decken sich mit dem automatischen Copilot-Review; Punkt 3 wurde zus\u00e4tzlich unabh\u00e4ngig verifiziert.\n\nFazit: Produktivcode kann so bleiben; die drei Testpunkte bitte vor dem Merge beheben.","gate":"\u2014","stacking":"\u2014","pickable":false,"unlocks":3,"unmet":0,"prerequisites":[],"concern":null}}